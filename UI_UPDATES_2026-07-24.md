# 이번 세션 작업 정리 (2026-07-24)

## 1. 검색 기능 재설계

기존 "정확도/균형/의미" 3단계 알파 블렌딩 하이브리드 검색과 전역 BM25 캐시를 전부 제거하고,
단순한 2단 모드로 교체했습니다.

- **파일명 검색** (기본값): 파일명 문자열에 대한 대소문자 무시 부분일치. 관련도 점수 없이
  업로드 최신순 정렬.
- **유사 검색**: 임베딩(MiniLM) 코사인 유사도로 후보를 뽑고(임계값 없이 유사도 > 0이면 전부
  후보), 그 후보 안에서만 BM25로 재랭킹(RRF 결합)합니다. BM25는 새 후보를 추가하지 못하고
  순위만 조정하므로, 형태소 분석 특성상 "지식관리"→"지식"처럼 엉뚱한 문서가 끼어드는 문제가
  구조적으로 방지됩니다.

## 2. HWP 업로드 실패 근본 원인 해결

여러 차례 배포·재현을 거쳐 다음을 확인하고 수정했습니다.

1. (1차 오진) 파일명 인코딩 문제로 의심 → 로케일 수정, 효과 없었음
2. LibreOffice에는 HWP 가져오기 필터가 아예 없다는 것을 확인 → **H2Orestart** 확장 설치 필요
3. 확장 설치(`unopkg add`)가 Java를 못 찾아 빌드 자체가 실패 → **libreoffice-java-common**
   패키지(단순 JRE로는 부족) 추가로 최종 해결, 실제 업로드로 재현·검증 완료

## 3. 불필요한 폴링 제거

- 검색 화면 "둘러보기" 모드의 8초 자동 폴링 → 수동 새로고침 버튼으로 교체
- 알림 안읽음 개수 폴링(20초 → 120초로 완화 후) → 완전 제거, React Query 기본 동작
  (창 포커스 복귀 시 자동 확인)으로 대체. 클라이언트 수가 늘어날수록 서버 부하가 커지는
  구조적 문제를 근본적으로 해소했습니다.

## 4. 접근 권한 버그 수정

검색·업로드 화면(일반 사용자용)이 로그인 도입 이전 MVP 시절 코드가 남아 관리자 전용
`/admin/stats`, `/admin/documents`를 그대로 호출하고 있어 일반 계정에서 403이 발생하던
문제를 발견. 관리자 엔드포인트를 그대로 열어주는 대신, 부서별 읽기 권한을 지키는 별도의
공개 엔드포인트(`/stats`, `/documents/recent`)를 새로 만들어 교체했습니다.

## 5. 업로드 "덮어쓰기" → "버전 추가"

사내 문서 특성상 같은 파일명이어도 내용이 다르면(혹은 같아도) 기존 문서를 지우지 않고
새 버전으로 올라가도록 변경했습니다.

- 동일 파일명 재업로드 시 기존 문서를 삭제하지 않고 그대로 둔 채 새 문서로 추가
- 완전히 동일한 내용(SHA-256 동일)이어도 업로드 자체는 허용하고, 팝업으로만 안내
- (자체 발견) 같은 파일명으로 저장 경로가 겹쳐 기존 파일의 실제 바이트가 덮어써지는 문제를
  미리 발견해 저장 경로에 "(2)", "(3)" 접미사를 붙이는 방식으로 방지
- 업로드 후 뜨는 "카테고리·특이사항 설정" 팝업을 카테고리 미지정 상태로 닫을 수 없도록 변경
  (X/배경 클릭/ESC 전부 차단, 카테고리 선택 전엔 저장 버튼 비활성화)
- 같은 파일명 재업로드 시 확인 팝업이 이중으로 뜨던 버그 수정(Upload 컴포넌트 중첩 구조 분리 +
  파일명 단위 방어 잠금)

## 6. 관리자 "확인 필요 문서" 탭 (구 "실패 문서")

- 원래 "실패 문서" 탭이 실제로는 OCR 저신뢰 문서를 보여주고 있던 버그를 먼저 수정(파싱
  실패 문서만 보이도록)
- 이후 요청에 따라 파싱 실패 문서 **+** OCR 검토가 필요한 문서를 함께 모아 보여주도록 확장하고,
  탭 이름을 "확인 필요 문서"로 변경(실제 내용에 맞지 않던 "실패 문서"라는 이름 정리)

## 7. 카테고리 삭제 전 문서 이동

카테고리를 삭제하면 안에 있던 문서가 전부 자동으로 "보고서"로 재분류되던 기존 방식(소프트
삭제 부작용)을 없애고, 다음과 같이 재설계했습니다.

- 카테고리는 **문서가 0개일 때만** 삭제 가능(문서가 남아있으면 삭제 버튼 비활성화 + 안내)
- 삭제 전에 관리자가 원하는 다른 카테고리로 문서 전체를 옮길 수 있는 **"이동"** 기능 추가

## 8. 삭제 감사 로그 — "삭제 보고서"

관리자가 문서를 삭제할 때 **누가/언제/무엇을/왜** 지웠는지 남기는 감사 로그 기능을
새로 만들었습니다.

- 관리자 단건/일괄 삭제 시 **삭제 사유 입력을 필수화**(빈 값이면 거부)
- 관리자 전용 **"삭제 보고서"** 화면에서 파일명·카테고리·삭제한 관리자·사유·삭제 시각 확인 가능
- 삭제 직후 정확한 사유를 몰라 임시로 남긴 경우를 위해, **나중에 사유를 수정**할 수 있는
  인라인 편집 기능 추가
- 기록이 무한정 쌓이지 않도록 **3개월(90일)** 지난 로그는 매일 자동 정리
- "삭제 보고서" 탭을 "확인 필요 문서" 바로 오른쪽에 배치

## 9. 그 밖의 UI 다듬기

- 삭제 사유 입력창의 글자수 카운터(0/300)가 어색하게 떠 보이던 간격 문제 수정, 입력창
  크기와 안내 문구도 공간을 더 잘 쓰도록 조정
- 카테고리 이동 모달에서 통계 데이터 로딩 전 접근 시 발생하던 크래시 수정

## 배포 상태

전부 `km/main`에 반영 완료, Railway에 배포되어 있습니다.
