# ── 서버 / DB ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
python-multipart>=0.0.9

# ── 로그인(구글 OAuth, 회사 도메인 제한) ──────────────────────────────────────
google-auth>=2.29.0   # 구글 ID 토큰 서명 검증
requests>=2.32.0      # google-auth의 기본 HTTP 트랜스포트가 요구함
itsdangerous>=2.2.0   # 자체 세션 쿠키 서명
python-dotenv>=1.0.0  # backend/.env(git 미추적)에서 GOOGLE_CLIENT_ID 등 로드

# aiofiles·alembic은 현재 코드에서 미사용이라 제거 (2026-07-04 C6 점검).
# alembic은 설계도의 "Lifespan 7단계(alembic→…)" 도입 시 다시 추가할 것.

# ── 검색 / 임베딩 ────────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
elasticsearch>=8.15.0,<9.0.0   # 2026-07-28: turbovec+즉석 BM25+손으로 짠 RRF를 대체
# rank-bm25·kiwipiepy는 제거(2026-07-30) — search.py의 BM25Indexer가 호출부 없는 죽은
# 코드라 함께 삭제했다. BM25는 ES 내장 Lucene BM25가 담당한다.

# ── 문서 파서 (core/parser.py, core/office_adapter.py, core/docling_parser.py) ──
# [설치 방법] torch CPU 버전은 아래 명령어로 설치하세요 (일반 pip install 하면 GPU 버전이 깔릴 수 있음):
# pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
numpy==2.4.4
torch==2.12.1+cpu

# [2026-07-28 교체] pdfplumber+camelot-py+img2table+tabulate(PDF)와
# python-docx+openpyxl+xlrd+lxml+LibreOffice(DOCX/PPTX/XLSX/HWP/HWPX)를
# Docling 하나로 통합했다. HWP/HWPX는 Docling이 지원하지 않아 지원을 포기했다.
# Docling은 PDF·DOCX·PPTX·XLSX(+ 구형 DOC/XLS/PPT)를 전부 자체 처리하며
# LibreOffice·Poppler 등 외부 시스템 바이너리가 필요 없다.
docling[easyocr]>=2.115.0  # MIT — 통합 파서 + OCR(EasyOCR 백엔드, 기존과 동일 엔진)
pypdfium2>=4.0.0           # BSD — count_pdf_pages() 사전 페이지수 체크 (Docling도 내부적으로 씀)

# ↓ Docling의 전이 의존성 — 우리 코드가 직접 import하지는 않지만(core/parser.py가 직접
#   쓰는 건 pypdfium2뿐이다) Docling 내부에서 쓰이므로 버전을 고정해 재현성을 확보한다.
pillow==12.3.0             # HPND(허용) — 이미지 처리
pandas==3.0.3              # BSD-3(허용) — 표(DataFrame) 처리
